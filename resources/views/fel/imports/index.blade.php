@extends('layouts.app')
@section('title', 'Historial FEL')
@section('content')
<x-page-header title="Historial de importaciones FEL" subtitle="Lotes procesados para la empresa activa." icon="bi-clock-history"><x-slot:actions><x-action-button :href="route('fel-imports.create')" icon="bi-plus-lg">Nueva importación</x-action-button></x-slot:actions></x-page-header>
<div class="card shadow-sm mb-4"><div class="card-body"><form class="row g-3"><div class="col-md-2"><label class="form-label">Desde</label><input type="date" name="from" value="{{ request('from') }}" class="form-control"></div><div class="col-md-2"><label class="form-label">Hasta</label><input type="date" name="to" value="{{ request('to') }}" class="form-control"></div><div class="col-md-2"><label class="form-label">Origen</label><select name="source_type" class="form-select"><option value="">Todos</option>@foreach($sourceTypes as $type)<option value="{{ $type->value }}" @selected(request('source_type')===$type->value)>{{ $type->value }}</option>@endforeach</select></div><div class="col-md-2"><label class="form-label">Estado</label><select name="status" class="form-select"><option value="">Todos</option>@foreach($statuses as $status)<option value="{{ $status->value }}" @selected(request('status')===$status->value)>{{ $status->value }}</option>@endforeach</select></div><div class="col-md-3"><label class="form-label">Archivo</label><input name="filename" value="{{ request('filename') }}" class="form-control"></div><div class="col-md-1 d-flex align-items-end"><button class="btn btn-primary">Filtrar</button></div></form></div></div>
<x-table-container><table class="table table-hover align-middle mb-0"><thead><tr><th>Fecha</th><th>Archivo</th><th>Origen</th><th>Estado</th><th>Registros</th><th>Importados</th><th>Enriquecidos</th><th>Duplicados</th><th>Fallidos</th><th></th></tr></thead><tbody>@forelse($batches as $batch)<tr><td>{{ $batch->created_at->format('d/m/Y H:i') }}</td><td>{{ $batch->original_filename }}</td><td>{{ $batch->source_type->value }}</td><td><span class="badge text-bg-{{ $batch->failed_records ? 'warning' : 'success' }}">{{ $batch->status->value }}</span></td><td>{{ $batch->total_records }}</td><td>{{ $batch->successful_records }}</td><td>{{ $batch->enriched_records }}</td><td>{{ $batch->duplicate_records }}</td><td>{{ $batch->failed_records }}</td><td><a href="{{ route('fel-imports.show', $batch) }}" class="btn btn-sm btn-outline-primary">Ver</a></td></tr>@empty<tr><td colspan="10"><x-empty-state title="Sin importaciones" message="Todavía no se han procesado documentos FEL." /></td></tr>@endforelse</tbody></table></x-table-container><div class="mt-3">{{ $batches->links() }}</div>
@endsection
